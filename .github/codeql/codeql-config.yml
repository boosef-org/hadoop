name: "All the goodness from lgtm.com"

disable-default-queries: false

queries:
  ### NEW - the below suites are now available
  - uses: security-and-quality # all queries from security-extended, plus code quality queries
  # - uses: security-extended  # all queries tagged as "security"
